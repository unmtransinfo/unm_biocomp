*.class
**/target/
.settings
.project
.classpath
# ChemAxon dirs (license + local files) — never commit
**/.chemaxon/

# Secret-bearing config — keep local copies, never commit
conf/postgresql/.pgpass
conf/tomcat/tomcat-users.xml
